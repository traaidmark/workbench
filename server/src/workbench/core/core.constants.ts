

export const ERROR = {
  BOOTSTRAP: {
    noModule: 'You need to load at least one valid module.',
    hasNotRegistered: 'Cannot start the application without any valid registrations. Did you forget to call: .register()?',
  }
}